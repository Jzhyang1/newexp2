# YCSB Workload B: Read mostly.
# Reference: My-YCSB rocksdb/config/ycsb_b.yaml (op proportions read=0.95 update=0.05,
# request_distribution=zipfian, zipfian_constant=0.99).
#
# Same note as ycsb_a.txt: update ops collapse to plain reads here, so this produces
# the identical page-access pattern to ycsb_a.txt/ycsb_c.txt/ycsb_f.txt. That's
# expected -- this simulator's cache metrics only depend on which pages are touched,
# and A/B/C/F all touch pages the same way (Zipfian(0.99) reads).
zipfian alpha=0.99
zipfian alpha=0.99
zipfian alpha=0.99
zipfian alpha=0.99
