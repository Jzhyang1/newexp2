# YCSB Workload D: Read latest.
# Reference: My-YCSB rocksdb/config/ycsb_d.yaml (op proportions read=0.95 insert=0.05,
# request_distribution=latest).
#
# The insert side of this mix collapses into LatestWorkload's own key-generation
# scheme (a "latest" request draws a recently-inserted key with the same math the
# insert path uses to place it), so read-ratio directly matches My-YCSB's `read`
# proportion. This is the only workload here that touches pages differently from
# the plain-zipfian workloads (A/B/C/F).
latest read-ratio=0.95
latest read-ratio=0.95
latest read-ratio=0.95
latest read-ratio=0.95
