# YCSB Workload A: Update heavy.
# Reference: My-YCSB rocksdb/config/ycsb_a.yaml (op proportions read=0.5 update=0.5,
# request_distribution=zipfian, zipfian_constant=0.99).
#
# `dat` has no op-type-aware backend, and My-YCSB's own key generation is identical
# for READ/UPDATE (only SCAN differs), so the update half of this mix is represented
# as plain reads -- this reproduces the exact same page-access sequence real YCSB
# would generate, just without a separate "update" label. See README.md for details.
zipfian alpha=0.99
zipfian alpha=0.99
zipfian alpha=0.99
zipfian alpha=0.99
